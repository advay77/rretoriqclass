// approveUser.cjs: Approval script disabled
// The project previously had a CLI script to approve users by setting
// `users/{uid}.approved = true` in Firestore. That approval flow has been
// removed. This file is intentionally disabled to avoid accidental usage.

console.log('approveUser.cjs has been disabled. Approval tooling removed.')
process.exit(0)
